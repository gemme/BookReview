import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    'header': {
        textAlign: 'center',
        fontSize: 40,
        color: '#f44242',
        padding: 50,
        fontWeight: '300'

      }
});