import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    'header': {
        textAlign: 'center',
        fontSize: 30,
        color: '#f44242',
        padding: 50,
        fontWeight: '100'

      },
      'iOSheader': {
        textAlign: 'center',
        fontSize: 30,
        color: '#f44242',
        padding: 30,
        fontWeight: '200'

      }
});
